using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyMoveController : MonoBehaviour 
{

//    public IEnumerator MoveToPos(List<EnemyBase.Movement.ToPos> toPosList)
//    {
//        for (int i = 0; i < toPosList.Count; i++)
//        {
//            EnemyBase.Movement.ToPos currToPos = toPosList[i];
//            Vector2 pos = currToPos.targetPos;
//            float spd = currToPos.speed;
//            float delay = currToPos.endDelay;
//
//            transform.Translate(pos * spd * Time.deltaTime);
//
//            yield return new WaitForSeconds(delay);
//        }
//
//    }
}
